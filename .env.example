# =============================================================================
# AdAutonomy Environment Configuration
# Copy this file to .env.local (development) or .env (production)
# =============================================================================

# -----------------------------------------------------------------------------
# App
# -----------------------------------------------------------------------------
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=AdAutonomy

# Demo mode: auto-approve agent actions but log everything
DEMO_MODE=true

# -----------------------------------------------------------------------------
# LLM Provider (openai | mock)
# Use "mock" for fully offline operation with no API keys
# -----------------------------------------------------------------------------
LLM_PROVIDER=mock
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# -----------------------------------------------------------------------------
# Supabase (cloud)
# Dashboard: https://supabase.com/dashboard/project/YOUR_PROJECT_ID
# Run supabase/schema.sql in the SQL Editor to create tables
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-publishable-key
SUPABASE_SERVICE_ROLE_KEY=your-secret-key
SUPABASE_PROJECT_ID=your-project-id

# Direct PostgreSQL connection (Dashboard → Settings → Database)
# For psql, DBeaver, migrations — optional; the app uses supabase-js above
DATABASE_URL=postgresql://postgres:your-db-password@db.your-project-id.supabase.co:5432/postgres

# SQLite fallback when Supabase is unavailable
USE_SQLITE_FALLBACK=true
SQLITE_DB_PATH=./data/adautonomy.db

# -----------------------------------------------------------------------------
# PayPal Sandbox API
# Get credentials at https://developer.paypal.com/dashboard/applications/sandbox
# -----------------------------------------------------------------------------
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
NEXT_PUBLIC_PAYPAL_CLIENT_ID=
PAYPAL_SANDBOX_MODE=true
PAYPAL_API_BASE=https://api-m.sandbox.paypal.com

# -----------------------------------------------------------------------------
# Vercel Eve (offline agent runtime)
# -----------------------------------------------------------------------------
EVE_OFFLINE_MODE=true
EVE_AGENT_PATH=./eve

# -----------------------------------------------------------------------------
# Sponsor integrations (simulated locally)
# -----------------------------------------------------------------------------
MODAL_SIMULATION_ENABLED=true
# Deploy `modal/app.py`, then paste the generated Web Function URL here.
# Leave blank to use the deterministic local Monte Carlo fallback.
MODAL_SIMULATION_URL=
MODAL_SIMULATION_RUNS=5000
MODAL_REQUEST_TIMEOUT_MS=20000
# Optional Modal proxy-token authentication:
MODAL_PROXY_KEY=
MODAL_PROXY_SECRET=
SEAPOINT_WORKFLOW_ENABLED=true
# Seapoint's public site does not document a public API. The app therefore runs
# finance controls locally and can optionally deliver snapshots to an authorized
# private endpoint or automation bridge using this generic webhook contract.
SEAPOINT_WEBHOOK_URL=
SEAPOINT_WEBHOOK_TOKEN=
SEAPOINT_REQUEST_TIMEOUT_MS=10000
SEAPOINT_OPENING_CASH=250000
SEAPOINT_MONTHLY_BASELINE_BURN=18000
SEAPOINT_APPROVAL_THRESHOLD=1000
# Official Manus API v2 multi-agent planning
# Create a key at https://manus.im/app/settings/integrations
MANUS_ORCHESTRATION_ENABLED=true
MANUS_API_KEY=
MANUS_API_BASE=https://api.manus.ai
MANUS_AGENT_PROFILE=manus-1.6-lite
MANUS_PROJECT_ID=
MANUS_TASK_TIMEOUT_MS=60000
MANUS_POLL_INTERVAL_MS=2000
MANUS_HIDE_TASK=false
ELYOS_SAFETY_ENABLED=true
WASSIST_FEEDBACK_ENABLED=true
SEEDCAMP_INVESTOR_ENABLED=true
BLUEWIRE_FINANCE_ENABLED=true
IO_DEPLOYMENT_ENABLED=true
DRAGONFLY_SCORING_ENABLED=true

# -----------------------------------------------------------------------------
# Feature flags
# -----------------------------------------------------------------------------
ENABLE_PAYPAL_MOCK_FALLBACK=true
ENABLE_AGENT_LOGGING=true
ENABLE_ICS_EXPORT=true
ENABLE_PDF_EXPORT=true

# -----------------------------------------------------------------------------
# Social Media APIs (add later for live auto-posting)
# -----------------------------------------------------------------------------
META_ACCESS_TOKEN=
TIKTOK_ACCESS_TOKEN=
LINKEDIN_ACCESS_TOKEN=
TWITTER_BEARER_TOKEN=
YOUTUBE_API_KEY=
ENABLE_SOCIAL_AUTO_PUBLISH=true

# -----------------------------------------------------------------------------
# Personal Assistant (uses OPENAI_API_KEY above when LLM_PROVIDER=openai)
# -----------------------------------------------------------------------------
ASSISTANT_ENABLED=true

# -----------------------------------------------------------------------------
# Video background music (MP3 in public/audio/)
# Replace public/audio/background.mp3 with your own file
# -----------------------------------------------------------------------------
NEXT_PUBLIC_VIDEO_BACKGROUND_AUDIO=/audio/background.mp3
