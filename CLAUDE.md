# AdAutonomy — Claude Code

The full specification is maintained in one canonical file shared by all coding agents
(Cursor, Codex, Claude Code, OpenCode). Read it first:

@AGENTS.md

Do not duplicate the spec here. If guidance needs to change, edit `AGENTS.md` so every
tool stays in sync.
